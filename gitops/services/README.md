# services

Each file here is an ArgoCD `Application` for one service, deployed via
`gitops/charts/spring-boot-app`. Normally created for you by the
`spring-boot-service` Backstage template as a PR - see
`gitops/charts/spring-boot-app/README.md` for the manifest shape if adding
one by hand.
